name: tra420-modeling
channels:
  - conda-forge
  - defaults
dependencies:
  - python>=3.11
  - pip>=23
  # Core numerical stack (fill in as modules are implemented)
  - numpy>=1.26
  - pandas>=2.1
  - scipy>=1.11
  - openpyxl>=3.1
  # Visualization & UI (adjust when GUI tech is chosen)
  - matplotlib>=3.8
  - pip:
      # Placeholder for GUI libraries (e.g., streamlit, dash)
      - streamlit>=1.30
      # Climate module dependencies
      - fair>=2.2.2
      - pyyaml>=6.0
      # Ruff & developer tooling
      - ruff>=0.6.9
      - pre-commit>=3.7
